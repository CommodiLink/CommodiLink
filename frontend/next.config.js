// frontend/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Do NOT add output: 'export' here — we want a server-rendered app
};

module.exports = nextConfig;
